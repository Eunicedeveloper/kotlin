//package KotlinChallenge

//fun revArray(intOfArray: IntArray) {
   // val numsreversed = IntArray(intOfArray.size)

   // for (x in intOfArray) {
     //   numsreversed[x] = intOfArray[intOfArray.size - x - 1]

 //   }
   // return numsreversed
//}
 // fun main(args: Array<String>)
   // print( " Enter random number")
    //val usern= readLine()?.toInt()
    //val userinputArray = IntArray(usern)

   // for (i in 0 < until < usern)

        //userinputArray[i] = readLine()?.toInt()


  //  val reversal_Array = revArray(userinputArray)
   // print(reversal_Array)








// in counting array you start from 0, thus it will print the first number, 1 prints the second,-1 prints the last
// The above challenge is: Creating a function that takes an array of integers as input and returns the array in reverse
